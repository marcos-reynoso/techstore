import { ProductsPageSkeleton } from '@/components/products/product-loading'

// Este archivo se ejecuta automáticamente mientras se carga la página
export default function Loading() {
  return <ProductsPageSkeleton />
}